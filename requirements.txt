# YouTube Transcript Exporter -- runtime dependencies
#
# Core pipeline uses only the Python standard library; ytx imports these
# only when needed and falls back gracefully if missing.
#
# Required at runtime:
#   yt-dlp (installed via brew/pipx/winget, not pip in production runs)
#
# Optional, opt-in by feature:
#   pyyaml          -- nicer YAML frontmatter in markdown output
#   faster-whisper  -- local fallback transcription for caption-less videos
#                      Needs ffmpeg dev headers; see INSTALL.md

pyyaml>=6.0
# faster-whisper >= 1.0      # uncomment if you want Whisper fallback at install time
